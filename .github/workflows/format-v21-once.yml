name: Format V21 Once

on:
  push:
    branches:
      - feature/arcane-redesign-v2

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/arcane-redesign-v2
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Format V2.1 files
        run: bunx prettier --write src/components/GridHoverBackground.tsx src/components/VoxelArcaneLogo.tsx src/components/ArcaneLoader.tsx src/components/MetaversePortalV2.tsx src/components/ProjectScrapbookOrbit.tsx src/routes/index.tsx src/lib/animation-runtime.test.ts
      - name: Commit formatting
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/GridHoverBackground.tsx src/components/VoxelArcaneLogo.tsx src/components/ArcaneLoader.tsx src/components/MetaversePortalV2.tsx src/components/ProjectScrapbookOrbit.tsx src/routes/index.tsx src/lib/animation-runtime.test.ts
          if ! git diff --cached --quiet; then
            git commit -m "style: format Arcane redesign v2.1"
            git push
          fi
